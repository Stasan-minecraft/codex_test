# codex_test

This repository contains a simple smart calculator implemented in
`smart_calculator.py`.

Run the calculator:

```bash
python3 smart_calculator.py
```

Type arithmetic expressions using operators `+`, `-`, `*`, `/`, `%`,
`**` and functions from the `math` module such as `sqrt` or `sin`.
The variable `ans` stores the last result.
